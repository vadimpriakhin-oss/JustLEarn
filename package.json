{
  "name": "justlearn",
  "version": "1.0.0",
  "description": "Interactive learning app with multiple quiz modes",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for plain HTML/JS project'",
    "cap:init": "npx cap init JustLEarn com.justlearn.app --web-dir .",
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap open ios",
    "cap:android": "npx cap open android"
  },
  "keywords": [
    "education",
    "quiz",
    "pwa",
    "learning"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
