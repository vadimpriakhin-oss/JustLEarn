{
  "name": "just-learn",
  "version": "1.0.0",
  "description": "Learn English for exam preparation",
  "scripts": {
    "build": "echo 'No build step required for static web app'",
    "build:ios": "npm run build && npx cap sync ios && npx cap build ios",
    "build:android": "npm run build && npx cap sync android && npx cap build android",
    "deploy:ios": "npm run build:ios && npx cap open ios",
    "deploy:android": "npm run build:android && npx cap open android",
    "sync": "npx cap sync",
    "sync:ios": "npx cap sync ios",
    "sync:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}