{
  "name": "justlearn",
  "version": "1.0.0",
  "description": "JustLEarn - an interactive Vanilla JS learning app",
  "scripts": {
    "start": "npx serve .",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}